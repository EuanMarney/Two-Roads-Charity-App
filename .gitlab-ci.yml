image: node

build-job:
  tags:
    - docker
  stage: build
  script:
    - npm install eslint-plugin-react@latest --save-dev
    - npx eslint --fix --ext .js,.jsx .


tests:
  tags:
    - docker
  stage: test
  script:
    - echo "Jest tests here"
