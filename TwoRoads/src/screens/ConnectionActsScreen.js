import React from "react";
import { Text } from "react-native";
import Layout from "../components/Layout";

const ConnectionActsScreen = () => {
  return (
    <Layout>
      <Text>This is the Acts of Connection page</Text>
    </Layout>
  );
};

export default ConnectionActsScreen;
